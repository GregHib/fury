package com.fury.game.npc.minigames.pest

import com.fury.game.content.global.minigames.impl.PestControl
import com.fury.game.world.map.Position

class Splatter(id: Int, tile: Position, spawned: Boolean, index: Int, manager: PestControl) : PestMonsters(id, tile, spawned, index, manager)