package com.fury.game.npc.minigames;

import com.fury.core.model.node.entity.actor.figure.mob.Mob;
import com.fury.game.world.map.Position;

public class AnimatedArmour extends Mob {
    public AnimatedArmour(int id, Position position, boolean spawned) {
        super(id, position, spawned);
    }
}
